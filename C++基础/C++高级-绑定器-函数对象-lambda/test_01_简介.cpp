
/*
* 绑定器和函数对象operator()和lambda表达式
* 
* 1、C++ STL中的绑定器（只能应用于二元函数对象，使用场景有限）
* - bind1st：把operator()的第一个形参变量绑定成一个确定的值
* - bind2nd：把operator()的第二个形参变量绑定成一个确定的值
* 
* 
* 2、bind和function机制（C++11引入）
* 
* 3、lambda表达式（底层依赖函数对象机制实现的）
*/
